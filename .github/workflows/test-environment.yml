name: Checkout and Run

on: [push]

jobs:
  checkout-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout the code
        uses: actions/checkout@v4

      - name: List files
        run: ls -la
