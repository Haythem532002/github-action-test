name: Test Pipeline 

on:
  push:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest      
    steps:
      - name: checkout code
        uses: actions/checkout@v4
      - name: test code
        run: echo 'testing'
  deploy:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Deploy code
        run: echo 'deploying'
        
